#include <stdio.h>
int main() {
  int n, sum = 0, number;
printf("Enter the number of elements: ");
  scanf("%d", &n);
 for (int i = 0; i < n; ++i) 
{
  printf("Enter element %d: ", i + 1);
    scanf("%d", &number);
 if (number % 2 != 0)
 {
      sum += number;
    }
  } printf("The sum of all odd numbers is: %d\n", sum);}
