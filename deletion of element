#include<stdio.h>
 int main()
 {
 	int a[]={23,75,74,49,87};
 	int pos,size;
 	size=sizeof(a)/sizeof(a[0]);
 	scanf("%d",&pos);
 	for(int i=pos;i<size-1;i++)
 	{
 		a[i]=a[i+1];
	 }
	 for(int i=0;i<size-1;i++)
 	{
 		printf("%d ",a[i]);
	 }
 }
